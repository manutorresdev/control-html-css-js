{
  "name": "04.ejercicio-css-1",
  "version": "1.0.0",
  "description": "Edita el fichero `style.css` creando las reglas de CSS necesarias para que la primera lista que aparece en el `index.html` se represente visualmente de esta forma:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server 'www' --files 'www'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.5"
  }
}
